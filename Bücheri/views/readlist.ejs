<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        body {
            background-image: url("background1.jpg");
            background-repeat: no-repeat;
            background-size: 100%;
        }

        h1 {
            color: white;
            text-align: center;
            text-shadow: 2px 2px black;
        }
    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Want to Read List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-2">
        <form method= "POST" action= '/home' >
            <button type= "submit" style="background-color: 
            transparent; border: none; overflow: hidden; 
            color: rgb(246, 194, 68); font-size: 26px;">AFNA Public Library</button>
        </form>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
            </ul>
            <form method="POST" action='/search' class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" name="Search" type="search" placeholder="Search"
                    aria-label="Search">
                    <button class="btn btn-outline-warning my-2 my-sm-0" type="submit" style= "margin-right: 7px;">Search</button>
                </form>
                <form method="POST" action='/logout' class="form-inline my-2 my-lg-0">
                    <button class="btn btn-outline-danger my-2 my-sm-0" type="submit">Sign Out</button>
                </form>
        </div>
       
    </nav>
    <div class="container">
        <h1>Want to Read List</h1>
    </div>
        <div class="grid">
            <% for(var book in results) { %>
            <!--vertical arrangement of books-->
            <div class="card" style="width: 15vw; display: inline-flex; border: 0px !important">
                <img src="<%= results[book] %>.jpg" class="card-img-top" alt="<%= book %>" width="10vw !important">
                <div class="card-body" style="background-color: #313030;">
                    <h5 class="card-title" style="color: #fcae2d"><%= book %></h5>
                    <a href="<%= results[book] %>" class="btn btn-warning my-2 my-sm-0" !important;">Go to Book</a>
            </div>
        </div>
            <% } %>
        </div>
    
</body>
</html>